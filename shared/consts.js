
(function(ctx){

    ctx.visibleTiles = 11; //number of tiles visible (square). OBS: must be odd
    ctx.regionTiles = 11; //number of tiles in a region (square)
    ctx.tileSize = 48; //tile width and height (in px)
    ctx.slotSize = 16; //slot width and height (in px)

})( typeof exports !== 'undefined'? exports: this.ZT["shared"].consts = {} );

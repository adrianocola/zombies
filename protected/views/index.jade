doctype html
html
   head
      script(src="//cdn.jsdelivr.net/jquery/2.1.1/jquery.min.js")
      script(src="//cdn.jsdelivr.net/underscorejs/1.7.0/underscore-min.js")
      script(src="/js/index.js")

      link(rel='stylesheet', href='/css/index.css')

   body
      div.map
         - for (var x = 0; x < 121; x++)
            div.slot
               div.thing.zombie(class="rotate#{_.random(1, 12)}")
               - if (x%2===0)
                  div.thing.zombie(class="rotate#{_.random(1, 12)}")
               - if (x%3===0)
                  div.thing.zombie(class="rotate#{_.random(1, 12)}")
               - if (x%4===0)
                  div.thing.zombie(class="rotate#{_.random(1, 12)}")
               - if (x%5===0)
                  div.thing.zombie(class="rotate#{_.random(1, 12)}")
               - if (x%6===0)
                  div.thing.zombie(class="rotate#{_.random(1, 12)}")
               - if (x%7===0)
                  div.thing.zombie(class="rotate#{_.random(1, 12)}")
               - if (x%8===0)
                  div.thing.zombie(class="rotate#{_.random(1, 12)}")
               - if (x%9===0)
                  div.thing.zombie(class="rotate#{_.random(1, 12)}")